# 💼 업무 말투 변환기 — AI 어시스턴트 개발 지침서 (AGENTS.md)

이 문서는 AI 어시스턴트(Antigravity)가 **업무 말투 변환기** 프로젝트를 지원하고 코드를 작성할 때 엄격히 준수해야 하는 개발 규칙과 프로젝트 개요를 정의합니다.

---

## 1. 프로젝트 개요 (Overview)

### 1-1. 서비스 정의
**업무 말투 변환기**는 사용자가 전달하고자 하는 일상적인 어조의 내용을 입력하면, 수신하는 대상에 맞추어 적절한 비즈니스 업무 언어로 자동으로 변환 및 출력해 주는 웹 서비스입니다.

### 1-2. 수신 대상별 어조 전략
| 대상 코드 | 대상 | 변환 스타일 및 프롬프트 방향 |
|---|---|---|
| `boss` | 상사 / 임원 | 격식 있고 공손한 경어체, 간결함 유지 |
| `colleague` | 타팀 동료 | 정중하면서도 협조적인 업무 어조 |
| `client` | 고객 / 외부 | 친절하고 신뢰감을 주는 서비스 어조 |
| `team` | 팀 내 동료 | 간결하고 실무적인 어조 |

### 1-3. 핵심 기능 (Scope)
* **텍스트 입력 및 수신 대상 선택 (4종)**
* **LLM API 연동 및 말투 변환**: FastAPI 서버를 거쳐 Upstage Solar-Pro3 API 호출
* **로딩 표시 및 결과 출력**
* **결과 복사**: 변환 완료된 텍스트 클립보드 복사 기능

---

## 2. 현재 프로젝트 구조 분석

프로젝트의 기본적인 개발 환경 구성(STEP 1)이 완료된 상태입니다.

```text
biztalk_antigravity/
├── .agents/                    # 에이전트 설정 디렉터리
├── .taskmaster/                # 태스크 마스터 설정 디렉터리
├── .git/                       # 깃 저장소
├── .gitignore                  # Git 제외 파일 설정 (.env, .venv 등 포함)
├── .env                        # 로컬 환경 변수 파일 (API 키 보관, git 제외)
├── .env.example                # 환경 변수 템플릿
├── 개요서_업무말투변환기.md     # 기획 및 프로그램 개요 문서
├── PRD_업무말투변환기.md       # 완료 조건 및 기술 요구사항 명세서 (PRD)
├── AGENTS.md                   # AI 어시스턴트 지침서 (본 파일)
├── backend/                    # 백엔드 개발 폴더 (생성 완료)
│   ├── .env.example            # 백엔드용 환경 변수 예시 (생성 완료)
│   └── requirements.txt        # 백엔드 의존성 패키지 목록 (생성 완료)
└── frontend/                   # 프론트엔드 개발 폴더 (생성 완료)
    ├── css/                    # Vanilla CSS 스타일시트 폴더 (생성 완료)
    └── js/                     # JS 로직 폴더 (생성 완료)
```

### 💡 향후 구현해야 할 목표 디렉터리 구조
PRD에 정의된 최종 구조는 다음과 같습니다.
```text
biztalk_antigravity/
├── backend/
│   ├── main.py                 # FastAPI 앱 설정 & CORS 설정
│   ├── routers/
│   │   └── convert.py          # /api/convert 라우터
│   ├── services/
│   │   └── tone_converter.py   # LangChain + Solar-Pro3 연동
│   ├── prompts/
│   │   └── templates.py        # 대상별 프롬프트 템플릿
│   ├── models/
│   │   └── schemas.py          # Pydantic 요청/응답 모델
│   ├── .env                    # 백엔드용 환경 변수 (.gitignore 필수)
│   ├── .env.example            # 환경 변수 예시
│   └── requirements.txt        # 백엔드 의존성 패키지 목록
└── frontend/
    ├── index.html              # HTML 레이아웃
    ├── css/
    │   └── style.css           # Vanilla CSS 스타일시트
    └── js/
        └── app.js              # 프론트엔드 메인 로직 (API 호출 및 DOM 제어)
```

---

## 3. 개발 규칙 (Agent Guidelines)

AI 어시스턴트(Antigravity)는 프로젝트 진행 시 다음 규칙들을 철저히 준수해야 합니다.

### 3-1. 바이브 코딩 3원칙 (PRD 제1조 준수)
1. **원칙 1: 완료 기준을 먼저 정의하라**
   - 개발을 진행하기 전, 반드시 "무엇을 구현하면 끝나는지" 완료 체크리스트를 정의하고 그 기준에 맞추어 작업을 완료합니다. 범위 밖의 불필요한 기능(로그인, 이력 저장 등)은 임의로 추가하지 않습니다.
2. **원칙 2: 조사 먼저, 구현 나중**
   - 특히 Upstage Solar-Pro3 API와 LangChain의 연동 패키지(`langchain-upstage`) 버전을 다룰 때는, 먼저 연동 방식과 공식 명세를 조사하여 올바른 사용법을 확인한 후 코드를 작성합니다.
3. **원칙 3: 버그는 분석 먼저, 수정 나중**
   - 에러가 발생하면 무작정 수정 코드를 생성하지 않고, **에러의 원인을 먼저 설명하고 분석한 후** 사용자와 동의하에 수정 작업을 단계적으로 밟아 나갑니다.

### 3-2. 시스템 및 Git 관련 안전 규정
* **Git 파괴적 명령 금지**: `git push --force`, `git reset --hard` (특정 커밋 대상), `git clean -fd`, `git rebase` (공유 브랜치 대상)는 절대 자동 실행하지 않습니다.
* **민감 정보 보호**: `.env` 및 `.env.*` 설정 파일의 임의 수정이나 출력은 절대로 허용되지 않습니다.
* **사용자 명시적 승인 필요 (Warning)**: 패키지 설치(`npm install`, `pip install` 등), 브랜치 삭제, 마이그레이션 등 중요 영향력을 미치는 작업은 반드시 다음 템플릿을 사용하여 사용자 확인을 거칩니다:
  ```text
  ⚠️ [작업명]을 실행하려 합니다.
      영향 범위 : [파일 / 서비스 / 디렉토리명]
      예상 결과 : [작업 후 변경 사항 요약]
      계속하려면 'yes'를 입력하세요.
  ```

### 3-3. 프론트엔드 개발 가이드
* **Vanilla CSS 최우선**: TailwindCSS 등의 프레임워크는 사용자가 명시적으로 지시하지 않는 한 도입하지 않으며, 순수 CSS(Vanilla CSS)를 사용해 세련되고 직관적인 현대적 디자인을 구현합니다.
* **완벽한 데모 제공**: 이미지나 미디어 요소가 필요한 경우 임시 플레이스홀더를 사용하지 않고 실제 작동 가능한 데모 이미지를 제공합니다.
* **SEO 및 가독성 최적화**: 시맨틱 HTML5 태그 사용 및 각 인터랙션 요소에 고유 ID 부여를 기본으로 합니다.

### 3-4. 소통 및 보고 원칙
* **기본 언어**: 모든 소통 및 문서는 **한국어**로 작성합니다.
* **전문 용어 병기**: IT 업계 용어는 한국어 명칭과 영문 원어를 함께 표기합니다. (예: 가상환경(Virtual Environment))
* **진행 상황 보고**: 5단계 이상의 복잡한 태스크를 수행할 경우 진행 계획을 사전에 공유하며, 주기적으로 단계별 작업 결과를 요약 보고합니다.
* **파일 링크**: 모든 파일이나 코드 요소를 언급할 때는 반드시 Markdown의 `file://` 링크를 제공합니다.

## 4. 실행 및 테스트 (Running & Testing)

### 4.1. 환경 설정
```bash
# uv를 사용한 가상환경 생성 및 의존성 설치
uv venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r backend/requirements.txt
```

### 4.2. 백엔드 실행
```bash
cd backend
uvicorn main:app --reload --port 8000
```

### 4.3. 프론트엔드 확인
- `frontend/index.html`은 `http://localhost:8000` 으로 확인합니다. 
---

## 5. 보안 및 금지 사항 (Security & Restrictions)

- **API 키 보호**: `.env` 파일에 저장된 `UPSTAGE_API_KEY` 등이 코드에 노출되거나 커밋되지 않도록 주의합니다.
- **Git 조작**: `~/.gemini/AGENTS.md`에 명시된 Git 관련 금지 작업(강제 푸시, 히스토리 파괴 등)을 절대 수행하지 않습니다.
- **응답 언어**: 모든 설명과 주석은 **한국어**로 작성합니다.
---

### 6. @PRD_업무말투변환기.md 문서와 AGENTS.md 문서 항상 최신화 하기
* 모든 변경사항이 발생하면 (예를 들어 Source Code가 변경 되거나 라이브러리 버전이 변경되면) md 문서들도 반드시 업데이트 합니다. 
* 구현이 완료된 사항들은 `@PRD_업무말투변환기.md\2. 완료 체크리스트`에 모두 체크표시를 해서 완료 되었음을 반드시 표시하세요.
* `@PRD_업무말투변환기.md\8. 단계별 구현 순서` 에서도 STEP별로 구현이 완료되면 체크표시를 해서 완료 되었음을 반드시 표시하세요.